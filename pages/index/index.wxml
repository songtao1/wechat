<!--index.wxml-->
<view class="mpcontainer">
	<map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="16" style="width: 100%; height: 100%;" show-location controls="{{controls}}" bindcontroltap="handleControlTap" markers='{{markers}}' bindmarkertap="bindMarkerTap">
		
	</map>
</view>
<view class="footercontainer">
	<navigator url="/pages/publish/publish" class="publish">发布</navigator>
 	<navigator url="/pages/search/search" class="search">搜索</navigator>
</view>

